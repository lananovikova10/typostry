# Bash commands
- npm run build: Build the project
- npm run dev: Run the dev mode

# Code style
- Destructure imports when possible (eg. import { foo } from 'bar')

# Workflow
- After finishing the task, run the application to check that there is no errors

